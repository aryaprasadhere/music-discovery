# Backend
PORT=4000
NODE_ENV=development

# If later use a DB
DATABASE_URL=postgres://user:pass@host:5432/dbname

# JWT or other secrets (do NOT commit real values)
JWT_SECRET=your_jwt_secret_here

# If later use a provider that needs a key
DEEZER_API_KEY= # (Deezer's public search doesn't need a key)
